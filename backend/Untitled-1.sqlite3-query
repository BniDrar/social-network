-- database: ../../../../../path/to/database.db
SELECT
    id,
    title,
    image,
    content,
    user_id,
    status,
    "group",
    created_at,
    updated_at
FROM
    post as ps
    JOIN group_members gm ON 1 = gm.member_id
    AND ps."group" = gm.group_id;
